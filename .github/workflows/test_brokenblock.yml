name: Test Broken Block 

on: [push, pull_request]

jobs:
  unit:
    name: Test broken block 13298824
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v2
    - name: Install Go + Node
      run: |
        sudo apt-get update
        sudo apt-get -y --no-install-recommends install golang-1.16 nodejs npm
    - name: Install yarn
      run: |
        npm install --global yarn
        yarn install
    - name: Run broken block (cache)
      run: |
        ./run.sh 13298824
